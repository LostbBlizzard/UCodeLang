%ULang:
  //has built in alias in compiler.Ex: int^ and unq int();
  $Unique_ptr<T>;

  //has built in alias in compiler.Ex: int[^] and unq int[?]; 
  $Unique_Array<T>;

  //has built in alias in compiler.Ex: int$ and shr int();
  $Shared_ptr<T>;

  //has built in alias in compiler.Ex: int[$] and shr int[?];
  $Shared_Array<T>;

   